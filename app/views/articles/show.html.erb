<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>スクレイピングアプリケーション</title>
    <style>
        header {
            background-color: gray;
            height: 300px;   
        }
        h1 {
            font-size: 2rem;
            color: white;
            text-align: center;
            padding:50px;
        }
        table {
            width: 80%; /* テーブルの幅を80%に設定 */
            margin: 20px auto; /* 左右中央に配置 */
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid black;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f0f0f0;
        }
        form {
          display: flex;
          justify-content: center;
          align-items: center;
          margin: 0;
        }
        input[type="url"] {
            width: 30%;
            height: 20px;
            padding: 10px;
            margin-bottom: 16px;
            border: 1px solid #ccc;
            border-radius: 3px;
            font-size: 16px;
        }
        button {
            width: 10%;
            padding: 10px;
            margin: 0px 20px 16px;
            color: black;
            border:1px solid #ccc;
            border-radius: 4px;
            cursor: pointer;
        }
    </style>
</head>
<body>
  <header >
    <h1>スクレイピングアプリケーション</h1>
    <form action="/submit-url" method="post">
        <input type="url" id="url" name="url" placeholder="スクレイピングするURLを入力" required>
        <button type="submit">実行</button>
    </form>
  </header>
  <main>
    <table>
        <thead>
            <tr>
                <th>タイトル</th>
                <th>日時</th>
                <th>本文</th>
            </tr>
        </thead>
        <tbody>
          <% @articles.each do |article| %>
          <tr>
            <td><%= article.title %></td>
            <td><%= article.time %></td>
            <td><%= article.content %></td>
          </tr>
          <% end %>
        </tbody>
    </table>
  </main>
</body>
</html>
